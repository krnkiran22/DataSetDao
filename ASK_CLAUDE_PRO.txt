# COPY THIS TO CLAUDE PRO

I'm building a Sui blockchain NFT minting application using TypeScript. I'm getting a `RangeError: Invalid array length` when trying to pass a `vector<u8>` parameter to my Move contract.

## Move Contract (Simplified)
```move
public entry fun mint_certificate_nft(
    dataset_title: String,
    // ... other params (all working fine) ...
    image_url_bytes: vector<u8>,  // ❌ THIS PARAMETER IS FAILING
    // ... more params ...
    clock: &Clock,
    ctx: &mut TxContext
) {
    let image_url = url::new_unsafe_from_bytes(image_url_bytes);
    // ... rest of function
}
```

## TypeScript Code (Failing)
```typescript
import { TransactionBlock } from '@mysten/sui.js/transactions';
import { bcs } from '@mysten/sui.js/bcs';

// Convert URL string to bytes
const imageUrl = 'https://ipfs.io/ipfs/QmZ4tDuvesekSs4qM5ZBKpXiZGun7S2CYtEZRB3DYXkjGx';
const imageUrlBytes = new Uint8Array(new TextEncoder().encode(imageUrl));

// Serialize using BCS
const serializedImageBytes = bcs.vector(bcs.U8)
    .serialize(Array.from(imageUrlBytes))
    .toBytes();

// Build transaction
const tx = new TransactionBlock();
tx.moveCall({
    target: `${PACKAGE_ID}::${MODULE_NAME}::mint_certificate_nft`,
    arguments: [
        tx.pure(metadata.title, 'string'),           // ✅ Works
        // ... other working params ...
        tx.pure(serializedImageBytes),               // ❌ FAILS: RangeError: Invalid array length
        // ... rest never reached ...
    ],
});
```

## Error
```
RangeError: Invalid array length
    at ulebEncode (uleb.ts:15:10)
    at BcsWriter.writeULEB (writer.ts:145:3)
    at BcsWriter.writeVec (writer.ts:157:8)
    at Object.Pure (Inputs.ts:54:11)
```

## What I've Tried (All Failed)
1. `tx.pure(imageUrlBytes, 'vector<u8>')`
2. `tx.pure([...imageUrlBytes], 'vector<u8>')`
3. `tx.pure(Array.from(imageUrlBytes), 'vector<u8>')`
4. `tx.pure(bcs.vector(bcs.U8).serialize(bytes).toBytes())`

## Dependencies
- @mysten/sui.js: v0.54.1
- @mysten/dapp-kit: v0.19.8

## Question
**How do I correctly pass a `vector<u8>` parameter to a Sui Move contract using @mysten/sui.js?**

All other parameter types (String, u64, u8, &Clock) work perfectly. Only `vector<u8>` is failing.

Alternative: Should I change the Move contract to accept `String` instead of `vector<u8>`? If so, how?
